/* now done in widchk.i
 setframefocus(frame {&frame-name}:handle).
 &if DEFINED(UIB_is_Running) NE 0 &then  
&if defined(childprogram) = 0  &then
     run initialise no-error. 
&endif
 &endif           
*/

                       
